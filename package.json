{
  "name": "instagram-login-clone",
  "private": true,
  "version": "1.0.0",
  "description": "Railway root package for full-stack deployment",
  "scripts": {
    "postinstall": "npm --prefix backend install --omit=dev",
    "start": "npm --prefix backend start"
  }
}
